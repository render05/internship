# Assignment 5 - Address Book CSV + SQLite CRUD

## Files
- `db_assignment5.py` : creates SQLite tables, inserts records, performs SELECT/UPDATE/DELETE.
- `address_book.csv` : sample address book data.
- `TODO_db_assignment5.md` : progress checklist.

## Run
From project root:
```bash
python assignment/assignment5/db_assignment5.py
```

It will use `db1.db` in the repository root (as configured in the script).

